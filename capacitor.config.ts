import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "in.co.triventure",
  appName: "beta-app",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
